# Proba configuration — copy to .env (all optional; defaults work out of the box)

# Path to the SQLite store (the canonical spine). Default: .proba/proba.db
# PROBA_DB=.proba/proba.db

# Where emitted artifacts (Playwright/Gherkin) and screenshots are written. Default: .proba
# PROBA_OUT=.proba
